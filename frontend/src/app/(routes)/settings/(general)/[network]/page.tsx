'use client';

import React from 'react';
import '../../settings.css';

const page = () => {
  return (
    <div className="mt-10">
      <div>Please select all networks to view access general settings</div>
    </div>
  );
};

export default page;
