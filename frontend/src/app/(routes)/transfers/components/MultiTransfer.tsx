import React from 'react';

const MultiTransfer = ({ chainID }: { chainID: string }) => {
  return (
    <div className="w-full h-full flex pt-32 justify-center">
      - MultiTransfer {chainID}-
    </div>
  );
};

export default MultiTransfer;
